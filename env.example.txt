# ─── MakerPlan environment variables ─────────────────────────────────────────
# 1. Copy this file:  cp .env.example .env
# 2. Replace the value below with your real Groq API key
# 3. Get one at https://console.groq.com → API Keys → Create Key → set Expiry: Never
# 4. NEVER commit .env to Git — it is already listed in .gitignore

GROQ_API_KEY=your_groq_api_key_here
